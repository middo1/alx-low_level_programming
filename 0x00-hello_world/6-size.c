#include <stdio.h>

/**
* main - entry point
* Return: always 0
*/
int main(void)
{
char a;
int b;
long int c;
long long int d;
float e;

printf(sizeof(a));
printf(sizeof(b));
printf(sizeof(c));
printf(sizeof(d));
printf(sizeof(e));
}
